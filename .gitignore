# 依赖与构建产物
node_modules/
dist/
build/
.astro/
.cache/
.pnpm-store/
*.log

# 构建脚本会把 Pagefind 索引写回 public/，属产物而非源码
public/pagefind/

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# 本地环境与凭证（始终不提交）
.env
.env.*
!.env.example

# Agent 集成产物（由 specify init 生成，可随时重装，不属于本项目内容）
.dsh/

# 模板验证用的临时工作区（验证结束后删除）
.verify/
